<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Funil Invisível™ — Clientes Todos os Dias Pelo Instagram</title>
    <meta name="description" content="O Método de 3 Passos que transforma seu Instagram em uma máquina de clientes. Sem gastar com anúncios, sem fazer dancinha, sem precisar de milhões de seguidores.">
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns=&quot;http://www.w3.org/2000/svg&quot; viewBox=&quot;0 0 100 100&quot;><text y=&quot;.9em&quot; font-size=&quot;90&quot;>🔓</text></svg>">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800;900&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <link rel="preload" as="image" href="mentor-imagev2.webp" fetchpriority="high">

    <!-- Meta Pixel Code -->
    <script>
    !function(f,b,e,v,n,t,s)
    {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
    n.callMethod.apply(n,arguments):n.queue.push(arguments)};
    if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
    n.queue=[];t=b.createElement(e);t.async=!0;
    t.src=v;s=b.getElementsByTagName(e)[0];
    s.parentNode.insertBefore(t,s)}(window, document,'script',
    'https://connect.facebook.net/en_US/fbevents.js');
    fbq('init', '1338519980495382');
    fbq('track', 'PageView');
    </script>
    <noscript><img height="1" width="1" style="display:none"
    src="https://www.facebook.com/tr?id=1338519980495382&ev=PageView&noscript=1"
    /></noscript>
    <!-- End Meta Pixel Code -->

    <style>
        /* ================= DESIGN SYSTEM ================= */
        :root {
            --bg-base: #05050A;
            --bg-card: rgba(17, 24, 39, 0.6);
            --bg-card-solid: #0B0F19;
            --glow-blue: #10B981;
            --glow-purple: #7C3AED;
            --action-main: #FF4500;
            --action-light: #FF8C00;
            --safe-green: #10B981;
            --border-light: rgba(255, 255, 255, 0.06);
            --border-hover: rgba(255, 255, 255, 0.15);
            --text-main: #F8FAFC;
            --text-muted: #94A3B8;
            --ff-heading: 'Outfit', sans-serif;
            --ff-body: 'Plus Jakarta Sans', sans-serif;
            --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
            --ease-spring: cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; font-size: 16px; }
        body { font-family: var(--ff-body); background: var(--bg-base); color: var(--text-main); line-height: 1.6; overflow-x: hidden; -webkit-font-smoothing: antialiased; padding-bottom: 60px; }
        ::selection { background: var(--action-main); color: #fff; }
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: var(--bg-base); }
        ::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.15); border-radius: 4px; }

        .ambient-light { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; pointer-events: none; z-index: -1; background: radial-gradient(circle at 15% 40%, rgba(16, 185, 129, 0.08), transparent 50%), radial-gradient(circle at 85% 60%, rgba(5, 150, 105, 0.06), transparent 50%); }
        .grid-pattern { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: -1; background-image: linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px); background-size: 50px 50px; mask-image: radial-gradient(ellipse at center, black 0%, transparent 80%); -webkit-mask-image: radial-gradient(ellipse at center, black 0%, transparent 80%); }

        .container { max-width: 1200px; margin: 0 auto; padding: 0 24px; }
        .text-gradient { background: linear-gradient(135deg, #FFFFFF 0%, #93C5FD 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .text-accent { background: linear-gradient(135deg, var(--action-light) 0%, var(--action-main) 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; font-weight: 800; }

        /* ================= CREDIBILITY BAR ================= */
        .credibility-bar { background: rgba(16, 185, 129, 0.06); border-bottom: 1px solid rgba(16, 185, 129, 0.12); padding: 12px 20px; text-align: center; font-size: 13px; color: rgba(255,255,255,0.6); font-weight: 500; }
        .credibility-bar strong { color: #10B981; font-weight: 700; }

        /* ================= HERO ================= */
        .hero { position: relative; padding: 80px 0 60px; text-align: center; overflow: hidden; }
        .hero-tag { display: inline-flex; align-items: center; gap: 10px; padding: 8px 16px; background: rgba(255,255,255,0.03); border: 1px solid var(--border-light); border-radius: 100px; font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 1px; color: var(--text-muted); margin-bottom: 30px; }
        .hero-tag__dot { width: 8px; height: 8px; background: var(--safe-green); border-radius: 50%; box-shadow: 0 0 10px var(--safe-green); }

        .hero-title { font-family: var(--ff-heading); font-size: clamp(32px, 5vw, 56px); font-weight: 800; line-height: 1.15; margin-bottom: 24px; letter-spacing: -1px; max-width: 900px; margin-inline: auto; }
        .hero-subtitle { font-size: clamp(16px, 2.2vw, 20px); color: var(--text-muted); margin-bottom: 40px; font-weight: 400; max-width: 700px; margin-inline: auto; line-height: 1.7; }
        .hero-subtitle strong { color: #fff; font-weight: 700; }

        .hero-bullets { display: flex; flex-direction: column; gap: 14px; max-width: 600px; margin: 0 auto 50px; text-align: left; }
        .hero-bullet { display: flex; align-items: center; gap: 16px; font-size: 16px; font-weight: 500; background: var(--bg-card); padding: 14px 22px; border-radius: 14px; border: 1px solid var(--border-light); backdrop-filter: blur(10px); }
        .hero-bullet__check { width: 26px; height: 26px; background: rgba(16, 185, 129, 0.1); border: 1px solid var(--safe-green); color: var(--safe-green); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 13px; font-weight: bold; flex-shrink: 0; box-shadow: 0 0 10px rgba(16, 185, 129, 0.2); }

        /* VSL Placeholder */
        .vsl-placeholder { max-width: 700px; margin: 0 auto 50px; position: relative; border-radius: 16px; overflow: hidden; border: 1px solid var(--border-light); aspect-ratio: 16/9; background: linear-gradient(135deg, rgba(16,185,129,0.05), rgba(5,5,10,0.95)); display: flex; align-items: center; justify-content: center; cursor: default; }
        .vsl-placeholder__text { text-align: center; }
        .vsl-placeholder__icon { font-size: 48px; margin-bottom: 12px; opacity: 0.5; }
        .vsl-placeholder__label { font-size: 14px; color: var(--text-muted); }

        /* ================= COMMON ENEMY ================= */
        .enemy-box { max-width: 800px; margin: 20px auto 80px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.06); border-radius: 20px; padding: 40px 50px; text-align: left; }
        .enemy-tag { display: inline-flex; align-items: center; gap: 8px; background: rgba(239, 68, 68, 0.08); color: #EF4444; padding: 8px 18px; border-radius: 100px; font-size: 12px; font-weight: 800; text-transform: uppercase; letter-spacing: 1.5px; margin-bottom: 20px; border: 1px solid rgba(239, 68, 68, 0.15); }

        /* ================= MECHANISM (3 STEPS) ================= */
        .mechanism-section { padding: 80px 0; }
        .steps-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; margin-top: 50px; }
        .step-card { background: var(--bg-card); border: 1px solid var(--border-light); border-radius: 24px; padding: 36px 28px; text-align: center; position: relative; transition: all 0.3s; backdrop-filter: blur(12px); }
        .step-card:hover { border-color: rgba(16,185,129,0.3); transform: translateY(-5px); box-shadow: 0 20px 40px rgba(0,0,0,0.5); }
        .step-number { width: 48px; height: 48px; background: linear-gradient(135deg, #10B981, #059669); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-family: var(--ff-heading); font-size: 20px; font-weight: 900; color: #fff; margin: 0 auto 20px; box-shadow: 0 8px 20px rgba(16,185,129,0.3); }
        .step-card__icon { font-size: 36px; margin-bottom: 16px; }
        .step-card__title { font-family: var(--ff-heading); font-size: 20px; font-weight: 700; margin-bottom: 12px; color: #fff; }
        .step-card__desc { color: var(--text-muted); font-size: 14px; line-height: 1.6; }
        .mechanism-note { text-align: center; margin-top: 40px; padding: 24px; background: rgba(16,185,129,0.04); border: 1px dashed rgba(16,185,129,0.2); border-radius: 16px; max-width: 600px; margin-inline: auto; }
        .mechanism-note p { color: rgba(255,255,255,0.6); font-size: 15px; }
        .mechanism-note strong { color: #10B981; }

        /* ================= SECTIONS & FEATURES ================= */
        .section-padding { padding: 80px 0; }
        .section-header { text-align: center; margin-bottom: 50px; }
        .section-tag { display: inline-block; font-size: 13px; font-weight: 700; color: #34D399; text-transform: uppercase; letter-spacing: 2px; margin-bottom: 16px; padding: 6px 16px; background: rgba(16, 185, 129, 0.08); border-radius: 100px; }
        .section-title { font-family: var(--ff-heading); font-size: clamp(28px, 4.5vw, 44px); font-weight: 800; line-height: 1.15; letter-spacing: -1px; }

        .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 24px; }
        .feature-card { background: var(--bg-card); border: 1px solid var(--border-light); border-radius: 24px; padding: 36px 28px; transition: all 0.3s; backdrop-filter: blur(12px); position: relative; overflow: hidden; }
        .feature-card:hover { border-color: rgba(16,185,129,0.3); transform: translateY(-5px); box-shadow: 0 20px 40px rgba(0,0,0,0.5); }
        .feature-card__icon { width: 56px; height: 56px; background: rgba(255,255,255,0.05); border-radius: 14px; display: flex; align-items: center; justify-content: center; font-size: 26px; margin-bottom: 20px; border: 1px solid rgba(255,255,255,0.05); }
        .feature-card__title { font-family: var(--ff-heading); font-size: 20px; font-weight: 700; margin-bottom: 12px; }
        .feature-card__desc { color: var(--text-muted); font-size: 14px; line-height: 1.6; }

        /* ================= MENTOR ================= */
        .mentor-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
        .mentor-img-wrapper { position: relative; border-radius: 24px; overflow: hidden; border: 1px solid var(--border-light); }
        .mentor-img { width: 100%; display: block; filter: grayscale(10%) contrast(1.1); transition: 0.5s; }
        .mentor-bio { font-size: 17px; color: var(--text-muted); margin-bottom: 30px; line-height: 1.7; }
        .mentor-bio strong { color: #fff; }
        .mentor-stats { display: flex; gap: 30px; margin-bottom: 30px; }
        .mentor-stat { text-align: center; }
        .mentor-stat__num { font-family: var(--ff-heading); font-size: 28px; font-weight: 900; color: #10B981; }
        .mentor-stat__label { font-size: 12px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; font-weight: 600; }

        /* ================= SOCIAL PROOF ================= */
        .prints-masonry { column-count: 3; column-gap: 24px; margin-top: 40px; }
        .prints-masonry > div { break-inside: avoid; margin-bottom: 24px; }

        .mock-whatsapp { background: #0B0F19; border-radius: 20px; border: 1px solid var(--border-light); padding: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.5); position: relative; overflow: hidden; }
        .mock-whatsapp::before { content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 4px; background: linear-gradient(90deg, #128C7E, #25D366); opacity: 0.8; }
        .mock-wa-header { display: flex; align-items: center; gap: 12px; margin-bottom: 20px; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 15px; }
        .mock-wa-avatar { width: 40px; height: 40px; border-radius: 50%; object-fit: cover; flex-shrink: 0; }
        .mock-wa-name { color: #F8FAFC; font-weight: 600; font-size: 15px; display: block; line-height: 1.2; }
        .mock-wa-status { color: var(--text-muted); font-size: 11px; font-weight: 500; }

        .mock-bubble { background: #1E293B; color: #F8FAFC; padding: 12px 16px; border-radius: 12px 12px 12px 0; font-size: 14px; line-height: 1.45; margin-bottom: 10px; width: fit-content; max-width: 92%; box-shadow: 0 2px 5px rgba(0,0,0,0.2); }
        .mock-bubble-time { font-size: 10px; color: rgba(255,255,255,0.4); display: flex; align-items: center; justify-content: flex-end; gap: 4px; margin-top: 6px; }
        .mock-tick { color: #53BDEB; font-weight: 800; font-size: 12px; letter-spacing: -2px; }

        /* Audio Players */
        .audio-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; margin-top: 40px; }
        .audio-card { background: var(--bg-card); border: 1px solid var(--border-light); border-radius: 24px; padding: 30px; backdrop-filter: blur(12px); }
        .audio-card__user { display: flex; align-items: center; gap: 16px; margin-bottom: 20px; }
        .audio-card__avatar-img { width: 50px; height: 50px; border-radius: 50%; object-fit: cover; border: 2px solid rgba(255,255,255,0.15); }

        .wa-voice { display: flex; align-items: center; gap: 10px; background: #0B4A3F; padding: 8px 12px 8px 8px; border-radius: 12px; position: relative; min-height: 58px; }
        .wa-voice__left { position: relative; width: 46px; height: 46px; flex-shrink: 0; cursor: pointer; }
        .wa-voice__avatar { width: 46px; height: 46px; border-radius: 50%; object-fit: cover; transition: opacity 0.2s; }
        .wa-voice__mic { position: absolute; bottom: -2px; right: -2px; width: 18px; height: 18px; background: #25D366; border-radius: 50%; display: flex; align-items: center; justify-content: center; }
        .wa-voice__mic svg { width: 10px; height: 10px; fill: #fff; }
        .wa-voice__speed { display: none; width: 46px; height: 46px; border-radius: 50%; background: rgba(255,255,255,0.15); border: 2px solid rgba(255,255,255,0.3); color: #fff; font-size: 12px; font-weight: 800; font-family: var(--ff-body); align-items: center; justify-content: center; cursor: pointer; transition: 0.2s; }
        .wa-voice.playing .wa-voice__avatar { display: none; }
        .wa-voice.playing .wa-voice__mic { display: none; }
        .wa-voice.playing .wa-voice__speed { display: flex; }
        .wa-voice__btn { width: 34px; height: 34px; border-radius: 50%; background: transparent; border: none; cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
        .wa-voice__btn svg { width: 22px; height: 22px; fill: #E2E8F0; }
        .wa-voice__mid { flex: 1; display: flex; flex-direction: column; gap: 4px; min-width: 0; }
        .wa-voice__wave-wrap { position: relative; height: 28px; display: flex; align-items: center; }
        .wa-voice__wave { display: flex; align-items: center; gap: 1.5px; width: 100%; height: 100%; }
        .wa-voice__wave span { flex: 1; background: rgba(255,255,255,0.25); border-radius: 100px; min-height: 3px; transition: background 0.15s; }
        .wa-voice__wave span.played { background: #5EEAD4; }
        .wa-voice__wave span:nth-child(1)  { height: 5px; }
        .wa-voice__wave span:nth-child(2)  { height: 10px; }
        .wa-voice__wave span:nth-child(3)  { height: 18px; }
        .wa-voice__wave span:nth-child(4)  { height: 8px; }
        .wa-voice__wave span:nth-child(5)  { height: 24px; }
        .wa-voice__wave span:nth-child(6)  { height: 14px; }
        .wa-voice__wave span:nth-child(7)  { height: 26px; }
        .wa-voice__wave span:nth-child(8)  { height: 10px; }
        .wa-voice__wave span:nth-child(9)  { height: 20px; }
        .wa-voice__wave span:nth-child(10) { height: 7px; }
        .wa-voice__wave span:nth-child(11) { height: 16px; }
        .wa-voice__wave span:nth-child(12) { height: 24px; }
        .wa-voice__wave span:nth-child(13) { height: 12px; }
        .wa-voice__wave span:nth-child(14) { height: 22px; }
        .wa-voice__wave span:nth-child(15) { height: 6px; }
        .wa-voice__wave span:nth-child(16) { height: 15px; }
        .wa-voice__wave span:nth-child(17) { height: 5px; }
        .wa-voice__wave span:nth-child(18) { height: 20px; }
        .wa-voice__wave span:nth-child(19) { height: 10px; }
        .wa-voice__wave span:nth-child(20) { height: 18px; }
        .wa-voice__wave span:nth-child(21) { height: 4px; }
        .wa-voice__wave span:nth-child(22) { height: 14px; }
        .wa-voice__wave span:nth-child(23) { height: 8px; }
        .wa-voice__wave span:nth-child(24) { height: 22px; }
        .wa-voice__wave span:nth-child(25) { height: 6px; }
        .wa-voice__wave span:nth-child(26) { height: 16px; }
        .wa-voice__wave span:nth-child(27) { height: 10px; }
        .wa-voice__wave span:nth-child(28) { height: 24px; }
        .wa-voice__wave span:nth-child(29) { height: 7px; }
        .wa-voice__wave span:nth-child(30) { height: 12px; }
        .wa-voice__dot { position: absolute; width: 10px; height: 10px; background: #5EEAD4; border-radius: 50%; top: 50%; transform: translateY(-50%); left: 0%; transition: left 0.1s linear; z-index: 2; box-shadow: 0 0 4px rgba(94,234,212,0.5); }
        .wa-voice__info { display: flex; justify-content: space-between; align-items: center; }
        .wa-voice__time { font-size: 11px; color: rgba(255,255,255,0.5); font-variant-numeric: tabular-nums; font-weight: 500; }
        .wa-voice__check { font-size: 11px; color: #34D399; letter-spacing: -2px; font-weight: 800; }

        /* ================= WHAT YOU GET ================= */
        .module-list { max-width: 700px; margin: 0 auto; display: flex; flex-direction: column; gap: 16px; }
        .module-item { display: flex; align-items: flex-start; gap: 18px; background: var(--bg-card); border: 1px solid var(--border-light); border-radius: 16px; padding: 24px; transition: 0.3s; }
        .module-item:hover { border-color: rgba(16,185,129,0.2); }
        .module-item__icon { width: 48px; height: 48px; background: rgba(16,185,129,0.08); border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 22px; flex-shrink: 0; }
        .module-item__title { font-family: var(--ff-heading); font-size: 17px; font-weight: 700; margin-bottom: 6px; }
        .module-item__desc { font-size: 14px; color: var(--text-muted); line-height: 1.5; }
        .module-item__tag { display: inline-block; font-size: 11px; font-weight: 700; color: #FBBF24; background: rgba(251,191,36,0.1); padding: 3px 10px; border-radius: 100px; margin-top: 8px; }

        /* ================= PRICING ================= */
        .pricing-wrapper { max-width: 520px; margin: 0 auto; }
        .pricing-card { background: var(--bg-card-solid); border: 2px solid transparent; background-image: linear-gradient(var(--bg-card-solid), var(--bg-card-solid)), linear-gradient(135deg, #10B981, #059669); background-origin: border-box; background-clip: padding-box, border-box; border-radius: 32px; padding: 44px 40px; text-align: center; position: relative; box-shadow: 0 30px 60px rgba(0,0,0,0.8), 0 0 50px rgba(16, 185, 129, 0.1); }
        .pricing-badge { position: absolute; top: -15px; left: 50%; transform: translateX(-50%); background: linear-gradient(90deg, #10B981, #059669); color: #fff; font-size: 12px; font-weight: 800; padding: 6px 20px; border-radius: 100px; white-space: nowrap; box-shadow: 0 4px 10px rgba(16, 185, 129, 0.4); }

        .price-val { font-family: var(--ff-heading); font-size: 64px; font-weight: 900; line-height: 1; margin: 20px 0; display: flex; justify-content: center; align-items: flex-start; }
        .price-val span:first-child { font-size: 24px; margin-top: 10px; margin-right: 5px; }

        .value-stack-list { margin: 20px 0 16px; text-align: left; padding: 14px 16px; background: rgba(255,255,255,0.03); border-radius: 12px; }
        .value-stack-item { display: flex; justify-content: space-between; align-items: baseline; padding: 10px 0; border-bottom: 1px solid rgba(255,255,255,0.05); font-size: 14px; color: rgba(255,255,255,0.85); gap: 8px; font-weight: 500; }
        .value-stack-item:last-child { border-bottom: none; }
        .value-stack-item s { color: rgba(255,255,255,0.35); font-weight: 400; font-size: 12px; text-decoration: line-through; }
        .value-stack-item .vs-price { color: #34D399; font-weight: 700; font-size: 14px; }
        .value-stack-item .vs-prices { text-align: right; white-space: nowrap; flex-shrink: 0; display: flex; align-items: baseline; gap: 6px; }
        .value-stack-total { display: flex; justify-content: space-between; align-items: center; margin-top: 4px; padding-top: 10px; border-top: 1px solid rgba(255,255,255,0.08); font-weight: 700; font-size: 14px; color: rgba(255,255,255,0.6); }
        .value-stack-total s { color: rgba(239,68,68,0.7); font-size: 14px; }

        .pricing-list { text-align: left; margin: 24px 0; display: flex; flex-direction: column; gap: 14px; }
        .pricing-item { display: flex; gap: 12px; font-size: 15px; color: var(--text-muted); }
        .pricing-item svg { width: 20px; height: 20px; color: var(--safe-green); flex-shrink: 0; }

        /* ================= GUARANTEE ================= */
        .guarantee-box { max-width: 700px; margin: 0 auto; background: rgba(16,185,129,0.03); border: 1px solid rgba(16,185,129,0.12); border-radius: 24px; padding: 48px 40px; text-align: center; }

        /* ================= FAQ ================= */
        .faq-list { max-width: 700px; margin: 0 auto; display: flex; flex-direction: column; gap: 12px; }
        .faq-item { background: var(--bg-card); border: 1px solid var(--border-light); border-radius: 16px; overflow: hidden; transition: 0.3s; }
        .faq-item.open { border-color: rgba(16,185,129,0.2); }
        .faq-question { display: flex; justify-content: space-between; align-items: center; padding: 20px 24px; cursor: pointer; font-family: var(--ff-heading); font-size: 16px; font-weight: 600; color: #fff; user-select: none; gap: 16px; }
        .faq-question:hover { color: #10B981; }
        .faq-arrow { font-size: 18px; transition: transform 0.3s; flex-shrink: 0; color: var(--text-muted); }
        .faq-item.open .faq-arrow { transform: rotate(180deg); color: #10B981; }
        .faq-answer { max-height: 0; overflow: hidden; transition: max-height 0.4s ease, padding 0.3s ease; }
        .faq-item.open .faq-answer { max-height: 300px; }
        .faq-answer__inner { padding: 0 24px 20px; color: var(--text-muted); font-size: 15px; line-height: 1.7; }

        /* ================= FINAL CTA ================= */
        .final-cta { text-align: center; padding: 80px 0 40px; }

        /* ================= BUTTONS ================= */
        .btn-glow { display: inline-flex; justify-content: center; align-items: center; gap: 12px; width: 100%; padding: 20px; background: linear-gradient(135deg, var(--action-light), var(--action-main)); border: none; border-radius: 16px; color: #fff; font-family: var(--ff-heading); font-size: 16px; font-weight: 800; letter-spacing: 1px; text-transform: uppercase; cursor: pointer; transition: all 0.3s ease; box-shadow: 0 10px 25px -5px rgba(255, 69, 0, 0.5); position: relative; overflow: hidden; text-decoration: none; }
        .btn-glow::after { content: ''; position: absolute; top: -50%; left: -60%; width: 50%; height: 200%; background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent); transform: skewX(-20deg); animation: shine 3s infinite; }
        @keyframes shine { 100% { left: 200%; } }
        .btn-glow:hover { transform: translateY(-3px); box-shadow: 0 15px 35px -5px rgba(255, 69, 0, 0.7); }

        .btn-green { background: linear-gradient(135deg, #10B981, #059669); box-shadow: 0 10px 25px -5px rgba(16,185,129,0.5); }
        .btn-green:hover { box-shadow: 0 15px 35px -5px rgba(16,185,129,0.7); }

        /* ================= STICKY MOBILE CTA ================= */
        .sticky-mobile-cta { position: fixed; bottom: 0; left: 0; width: 100%; background: rgba(5, 5, 10, 0.95); backdrop-filter: blur(15px); border-top: 1px solid var(--border-light); padding: 15px 20px; z-index: 999; transform: translateY(100%); transition: transform 0.4s var(--ease-out); display: none; text-align: center; box-shadow: 0 -10px 30px rgba(0,0,0,0.8); }
        .sticky-mobile-cta.visible { transform: translateY(0); }
        .sticky-mobile-cta .btn-glow { width: 100%; padding: 16px; font-size: 14px; }

        .reveal { opacity: 0; transform: translateY(30px); transition: all 0.8s var(--ease-out); }
        .reveal.active { opacity: 1; transform: translateY(0); }

        /* ================= RESPONSIVE ================= */
        @media (max-width: 992px) { .prints-masonry { column-count: 2; } }
        @media (max-width: 768px) {
            .prints-masonry { column-count: 1; }
            .hero { padding: 50px 0 40px; }
            .hero-title { font-size: 32px; }
            .enemy-box { padding: 28px 22px; margin: 20px auto 40px; }
            .mentor-grid, .audio-grid { grid-template-columns: 1fr; gap: 30px; }
            .steps-grid { grid-template-columns: 1fr; gap: 20px; }
            .pricing-card { padding: 32px 24px; border-radius: 24px; }
            .guarantee-box { padding: 36px 24px; }
            .btn-glow { width: 100%; padding: 18px; font-size: 15px; }
            .sticky-mobile-cta { display: block; }
            body { padding-bottom: 90px; }
            .mentor-stats { gap: 20px; }
            .mechanism-section { padding: 60px 0; }
            .section-padding { padding: 60px 0; }
        }
    </style>
</head>
<body>

    <div class="ambient-light"></div>
    <div class="grid-pattern"></div>

    <!-- ================= 1. CREDIBILITY BAR ================= -->
    <div class="credibility-bar">
        Criado por quem já fez <strong>+30 mil vendas online</strong> &nbsp;|&nbsp; Mesmo método que ele usa todos os dias
    </div>

    <!-- ================= 2. HERO SECTION ================= -->
    <section class="hero" id="heroSection">
        <div class="container" style="position:relative; z-index:2;">
            <span class="hero-tag"><span class="hero-tag__dot"></span> Método Validado</span>

            <h1 class="hero-title reveal">
                O Método de 3 Passos Que Transforma Seu Instagram Em Uma <span class="text-accent">Máquina de Clientes</span>
            </h1>

            <p class="hero-subtitle reveal">
                Sem gastar com anúncios, sem fazer dancinha, sem precisar de milhões de seguidores. Funciona mesmo que você <strong>esteja começando do zero</strong>.
            </p>

            <!-- VSL PLACEHOLDER: Substituir por embed de vídeo quando disponível -->
            <!-- <div class="vsl-placeholder reveal">
                <div class="vsl-placeholder__text">
                    <div class="vsl-placeholder__icon">▶️</div>
                    <div class="vsl-placeholder__label">Assista a apresentação completa</div>
                </div>
            </div> -->

            <div class="hero-bullets reveal">
                <div class="hero-bullet"><span class="hero-bullet__check">✓</span> Ative direto do celular em 15 minutos por dia</div>
                <div class="hero-bullet"><span class="hero-bullet__check">✓</span> Zero investimento em tráfego pago ou ferramentas</div>
                <div class="hero-bullet"><span class="hero-bullet__check">✓</span> Clientes chegam pelo WhatsApp já prontos pra comprar</div>
            </div>

            <div class="reveal" style="max-width: 420px; margin: 0 auto;">
                <a href="#pricing" class="btn-glow btn-green">Quero Atrair Clientes Todos os Dias</a>
            </div>
        </div>
    </section>

    <!-- ================= 3. COMMON ENEMY ================= -->
    <section class="container reveal">
        <div class="enemy-box">
            <span class="enemy-tag">⚠️ POR QUE NADA FUNCIONOU ATÉ AGORA</span>
            <h2 style="font-family: var(--ff-heading); font-size: 26px; margin-bottom: 24px; color:#fff; line-height:1.35;">Você não tem um problema de esforço. Tem um problema de método.</h2>
            <p style="color: rgba(255,255,255,0.55); font-size: 16px; line-height: 1.9;">
                Você já tentou <b style="color:#fff;">postar todo dia</b> — reels, stories, carrossel — e mesmo assim os seguidores não viram clientes.<br><br>
                Você já pensou em <b style="color:#fff;">investir em tráfego pago</b>, mas o custo é alto e o retorno é incerto. Ou pior: já investiu e tomou prejuízo.<br><br>
                Você já dependeu de <b style="color:#fff;">indicação e boca-a-boca</b>, mas isso é imprevisível — um mês vai, o outro não vai.<br><br>
                O problema não é você. O problema é que ninguém te ensinou a <b style="color:#10B981;">criar um sistema previsível de atração de clientes</b>. Você estava usando o Instagram como vitrine, quando deveria estar usando como <b style="color:#10B981;">funil de vendas</b>.
            </p>
        </div>
    </section>

    <!-- ================= 4. MECHANISM: 3 STEPS ================= -->
    <section class="mechanism-section">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-tag">O Funil Invisível™</span>
                <h2 class="section-title">3 Passos Simples.<br>Clientes Todos os Dias.</h2>
            </div>

            <div class="steps-grid reveal">
                <div class="step-card">
                    <div class="step-number">1</div>
                    <div class="step-card__icon">🧲</div>
                    <h3 class="step-card__title">O Post Magnético</h3>
                    <p class="step-card__desc">Um tipo específico de conteúdo que atrai pessoas interessadas no que você vende — sem parecer propaganda. Funciona com poucos seguidores.</p>
                </div>
                <div class="step-card">
                    <div class="step-number">2</div>
                    <div class="step-card__icon">🎯</div>
                    <h3 class="step-card__title">O Gatilho Invisível</h3>
                    <p class="step-card__desc">Uma sequência estratégica que filtra os curiosos e faz apenas compradores reais chegarem até o seu WhatsApp. Automático e silencioso.</p>
                </div>
                <div class="step-card">
                    <div class="step-number">3</div>
                    <div class="step-card__icon">💰</div>
                    <h3 class="step-card__title">O Script de Fechamento</h3>
                    <p class="step-card__desc">Um roteiro de conversa testado em +30 mil vendas que transforma a mensagem no WhatsApp em dinheiro na conta. Sem ser "vendedor chato".</p>
                </div>
            </div>

            <div class="mechanism-note reveal">
                <p>O passo a passo completo é revelado na <strong>Masterclass Funil Invisível™</strong> — uma aula direta de 36 minutos onde você sai com o funil montado.</p>
            </div>
        </div>
    </section>

    <!-- ================= 5. FEATURES (WHY IT WORKS) ================= -->
    <section class="section-padding" style="background: rgba(255,255,255,0.02); border-top: 1px solid var(--border-light); border-bottom: 1px solid var(--border-light);">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-tag">Por Que Funciona</span>
                <h2 class="section-title">Simples. Rápido.<br>Sem Complicação.</h2>
            </div>

            <div class="features-grid reveal">
                <div class="feature-card">
                    <div class="feature-card__icon">💰</div>
                    <h3 class="feature-card__title">Custo Zero</h3>
                    <p class="feature-card__desc">Esqueça tráfego pago. A estratégia usa alcance orgânico inteligente. Você só precisa do celular e conexão com a internet.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-card__icon">⚡</div>
                    <h3 class="feature-card__title">15 Minutos por Dia</h3>
                    <p class="feature-card__desc">Não é sobre trabalhar mais. É sobre postar o gatilho certo. Uma rotina enxuta que gera leads qualificados o dia inteiro.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-card__icon">🎯</div>
                    <h3 class="feature-card__title">Cliente Pronto pra Comprar</h3>
                    <p class="feature-card__desc">As pessoas não vêm perguntar preço — vêm decididas. O método filtra curiosos automaticamente e atrai só quem quer comprar.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 6. MENTOR ================= -->
    <section class="section-padding">
        <div class="container mentor-grid">
            <div class="mentor-img-wrapper reveal">
                <img src="https://exousia.com.br/wp-content/uploads/2025/09/foto-lucas-12_1-scaled-e1757078614274.webp" alt="Lucas Vignoli" class="mentor-img" loading="lazy">
            </div>
            <div class="reveal">
                <span class="section-tag">Quem Criou o Método</span>
                <h2 class="section-title" style="margin-bottom: 20px;">Lucas Vignoli</h2>
                <div class="mentor-stats">
                    <div class="mentor-stat">
                        <div class="mentor-stat__num">R$ 10M+</div>
                        <div class="mentor-stat__label">Faturados</div>
                    </div>
                    <div class="mentor-stat">
                        <div class="mentor-stat__num">30.000+</div>
                        <div class="mentor-stat__label">Vendas</div>
                    </div>
                </div>
                <div class="mentor-bio">
                    <p>Não é guru de internet. É <strong>operador de vendas</strong>. Já faturou mais de R$ 10 milhões e fez mais de 30 mil vendas na internet — e agora te entrega o método exato que ele usa todos os dias.</p>
                    <p style="margin-top: 14px;">Nesta Masterclass cirúrgica de 36 minutos, ele abre a operação dele e te entrega o processo exato para atrair clientes pagantes todos os dias.</p>
                </div>
                <a href="#pricing" class="btn-glow btn-green" style="padding: 16px 32px; font-size: 14px; max-width: 320px;">Acessar o Método Agora</a>
            </div>
        </div>
    </section>

    <!-- ================= 7. SOCIAL PROOF: WhatsApp Prints ================= -->
    <section class="section-padding" style="background: rgba(255,255,255,0.01);">
        <div class="container">
            <div class="section-header reveal" style="margin-bottom: 30px;">
                <span class="section-tag" style="background: rgba(16, 185, 129, 0.1); color: var(--safe-green);">Resultados Reais</span>
                <h2 class="section-title" style="font-size: clamp(26px, 4vw, 40px);">Eles aplicaram.<br>Olha o que aconteceu.</h2>
                <p style="color: var(--text-muted); margin-top: 16px; font-size: 15px;">Resultados de alunos reais que aplicaram o Funil Invisível™</p>
            </div>

            <div class="prints-masonry reveal">
                <!-- Print 1 -->
                <div class="mock-whatsapp">
                    <div class="mock-wa-header">
                        <img src="joaomarcos.webp" alt="João Marcos" class="mock-wa-avatar">
                        <div><span class="mock-wa-name">João Marcos</span><span class="mock-wa-status">Prestador de serviço</span></div>
                    </div>
                    <div class="mock-bubble">Lucas, apliquei o script da aula ontem à noite no meu Instagram. <div class="mock-bubble-time">10:14 <span class="mock-tick">✓✓</span></div></div>
                    <div class="mock-bubble">Já recebi 3 mensagens no WhatsApp de gente querendo fechar. E o melhor: ninguém pechinhou. <div class="mock-bubble-time">10:15 <span class="mock-tick">✓✓</span></div></div>
                </div>

                <!-- Print 2 -->
                <div class="mock-whatsapp">
                    <div class="mock-wa-header">
                        <img src="Bruna_F.webp" alt="Bruna F." class="mock-wa-avatar">
                        <div><span class="mock-wa-name">Bruna F.</span><span class="mock-wa-status">Começou há 7 dias</span></div>
                    </div>
                    <div class="mock-bubble">Lucas, eu mal tinha 400 seguidores... <div class="mock-bubble-time">15:30 <span class="mock-tick">✓✓</span></div></div>
                    <div class="mock-bubble">Fiz minha primeira venda ontem seguindo o passo a passo. Tô em choque pq achei que não ia funcionar pra mim! <div class="mock-bubble-time">15:31 <span class="mock-tick">✓✓</span></div></div>
                </div>

                <!-- Print 3 -->
                <div class="mock-whatsapp">
                    <div class="mock-wa-header">
                        <img src="Ricardo.webp" alt="Ricardo" class="mock-wa-avatar">
                        <div><span class="mock-wa-name">Ricardo</span><span class="mock-wa-status">Dono de loja de roupas</span></div>
                    </div>
                    <div class="mock-bubble">Adaptei o funil pro Instagram da minha loja. Fiz o post no formato que você ensinou. <div class="mock-bubble-time">09:12 <span class="mock-tick">✓✓</span></div></div>
                    <div class="mock-bubble">Acordei com 5 directs pedindo pra fechar pedido. Nunca tinha acontecido isso no orgânico. <div class="mock-bubble-time">09:14 <span class="mock-tick">✓✓</span></div></div>
                </div>

                <!-- Print 4 -->
                <div class="mock-whatsapp">
                    <div class="mock-wa-header">
                        <img src="camiladentista.webp" alt="Camila" class="mock-wa-avatar">
                        <div><span class="mock-wa-name">Camila</span><span class="mock-wa-status">Dentista</span></div>
                    </div>
                    <div class="mock-bubble">Minha agenda de avaliações lotou pras próximas 2 semanas depois que comecei a usar o método. <div class="mock-bubble-time">14:22 <span class="mock-tick">✓✓</span></div></div>
                    <div class="mock-bubble">Melhor investimento que fiz pro meu consultório. Obrigada! <div class="mock-bubble-time">14:23 <span class="mock-tick">✓✓</span></div></div>
                </div>

                <!-- Print 5 -->
                <div class="mock-whatsapp">
                    <div class="mock-wa-header">
                        <img src="Felipe.webp" alt="Felipe M." class="mock-wa-avatar">
                        <div><span class="mock-wa-name">Felipe M.</span><span class="mock-wa-status">Marketing freelancer</span></div>
                    </div>
                    <div class="mock-bubble">Parei minhas campanhas no Facebook Ads semana passada pq o lead tava caro demais. <div class="mock-bubble-time">18:40 <span class="mock-tick">✓✓</span></div></div>
                    <div class="mock-bubble">Fiz o post no formato invisível e já vieram 8 leads quentes no whats. Orgânico puro. Não acreditei. <div class="mock-bubble-time">18:42 <span class="mock-tick">✓✓</span></div></div>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 8. AUDIO TESTIMONIALS ================= -->
    <section class="section-padding" style="background: rgba(16, 185, 129, 0.03); border-top: 1px solid rgba(16, 185, 129, 0.08);">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-tag" style="color: var(--safe-green); background: rgba(16, 185, 129, 0.1);">Ouça Você Mesmo</span>
                <h2 class="section-title">Depoimentos em Áudio<br>de Quem Aplicou.</h2>
            </div>

            <div class="audio-grid">
                <!-- Audio 1 -->
                <div class="audio-card reveal">
                    <div class="audio-card__user">
                        <img src="fthomem.webp" alt="Eduardo" class="audio-card__avatar-img">
                        <div>
                            <h4 style="font-size: 18px;">Eduardo</h4>
                            <span style="color: var(--text-muted); font-size: 14px;">Dono de negócio físico</span>
                        </div>
                    </div>
                    <div class="wa-voice" data-src="depoimentogabriel.mp3">
                        <div class="wa-voice__left">
                            <img src="fthomem.webp" class="wa-voice__avatar">
                            <div class="wa-voice__mic"><svg viewBox="0 0 24 24"><path d="M12 14c1.66 0 3-1.34 3-3V5c0-1.66-1.34-3-3-3S9 3.34 9 5v6c0 1.66 1.34 3 3 3z"/></svg></div>
                            <div class="wa-voice__speed">1.0x</div>
                        </div>
                        <button class="wa-voice__btn">
                            <svg class="icon-play" viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
                            <svg class="icon-pause" viewBox="0 0 24 24" style="display:none;"><path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/></svg>
                        </button>
                        <div class="wa-voice__mid">
                            <div class="wa-voice__wave-wrap">
                                <div class="wa-voice__wave"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></div>
                                <div class="wa-voice__dot"></div>
                            </div>
                            <div class="wa-voice__info">
                                <span class="wa-voice__time">0:00</span>
                                <span style="display:flex;align-items:center;gap:4px;"><span class="wa-voice__duration"></span> <span class="wa-voice__check">✓✓</span></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Audio 2 -->
                <div class="audio-card reveal">
                    <div class="audio-card__user">
                        <img src="ftmulher.webp" alt="Luana" class="audio-card__avatar-img">
                        <div>
                            <h4 style="font-size: 18px;">Luana</h4>
                            <span style="color: var(--text-muted); font-size: 14px;">Começou do zero</span>
                        </div>
                    </div>
                    <div class="wa-voice" data-src="depoimentolegal2.mp3">
                        <div class="wa-voice__left">
                            <img src="ftmulher.webp" class="wa-voice__avatar">
                            <div class="wa-voice__mic"><svg viewBox="0 0 24 24"><path d="M12 14c1.66 0 3-1.34 3-3V5c0-1.66-1.34-3-3-3S9 3.34 9 5v6c0 1.66 1.34 3 3 3z"/></svg></div>
                            <div class="wa-voice__speed">1.0x</div>
                        </div>
                        <button class="wa-voice__btn">
                            <svg class="icon-play" viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
                            <svg class="icon-pause" viewBox="0 0 24 24" style="display:none;"><path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/></svg>
                        </button>
                        <div class="wa-voice__mid">
                            <div class="wa-voice__wave-wrap">
                                <div class="wa-voice__wave"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></div>
                                <div class="wa-voice__dot"></div>
                            </div>
                            <div class="wa-voice__info">
                                <span class="wa-voice__time">0:00</span>
                                <span style="display:flex;align-items:center;gap:4px;"><span class="wa-voice__duration"></span> <span class="wa-voice__check">✓✓</span></span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 9. WHAT YOU GET ================= -->
    <section class="section-padding">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-tag">O Que Você Recebe</span>
                <h2 class="section-title">Tudo Que Está Incluso<br>No Seu Acesso.</h2>
            </div>

            <div class="module-list reveal">
                <div class="module-item">
                    <div class="module-item__icon">🎯</div>
                    <div>
                        <div class="module-item__title">Masterclass Funil Invisível™</div>
                        <div class="module-item__desc">Aula direta de 36 minutos onde você monta o funil completo do zero. Sem enrolação — sai da aula com tudo pronto pra ativar.</div>
                    </div>
                </div>
                <div class="module-item">
                    <div class="module-item__icon">🗣️</div>
                    <div>
                        <div class="module-item__title">MasterClass de Vendas no WhatsApp</div>
                        <div class="module-item__desc">Scripts testados em +30 mil vendas para converter mensagens em pagamentos. Copie, cole e adapte pro seu negócio.</div>
                        <div class="module-item__tag">Bônus Incluso</div>
                    </div>
                </div>
                <div class="module-item">
                    <div class="module-item__icon">🔥</div>
                    <div>
                        <div class="module-item__title">Fechamento Avançado</div>
                        <div class="module-item__desc">Técnicas para lidar com "vou pensar", "tá caro" e todas as objeções que travam a venda. Aumente sua taxa de conversão.</div>
                        <div class="module-item__tag">Bônus Incluso</div>
                    </div>
                </div>
                <div class="module-item">
                    <div class="module-item__icon">📈</div>
                    <div>
                        <div class="module-item__title">Entenda o Marketing de 2026</div>
                        <div class="module-item__desc">O que mudou, o que funciona agora e como se posicionar à frente da concorrência no cenário atual.</div>
                        <div class="module-item__tag">Bônus Exclusivo</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 10. PRICING ================= -->
    <section class="section-padding" id="pricing" style="background: rgba(0,0,0,0.5); border-top: 1px solid rgba(255,255,255,0.05);">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-tag">Acesso Imediato</span>
                <h2 class="section-title">Comece Agora.</h2>
            </div>

            <div class="pricing-wrapper reveal">
                <div class="pricing-card">
                    <div class="pricing-badge">COMBO COMPLETO</div>
                    <img src="mockup97.webp" alt="Combo Completo" style="width: 100%; display: block; margin: 10px auto 24px; border-radius: 12px;">

                    <div class="value-stack-list">
                        <div class="value-stack-item"><span>✓ Funil Invisível™ (Masterclass)</span><span class="vs-prices"><s>R$197</s> <span class="vs-price">Incluso</span></span></div>
                        <div class="value-stack-item"><span>✓ MasterClass de Vendas</span><span class="vs-prices"><s>R$97</s> <span class="vs-price">Incluso</span></span></div>
                        <div class="value-stack-item"><span>✓ Fechamento Avançado</span><span class="vs-prices"><s>R$97</s> <span class="vs-price">Incluso</span></span></div>
                        <div class="value-stack-item" style="color:#FBBF24;"><span>🎁 Bônus: Marketing 2026</span><span class="vs-prices"><s>R$97</s> <span class="vs-price">Incluso</span></span></div>
                        <div class="value-stack-total"><span>Valor se vendidos separados:</span><s>R$ 488</s></div>
                    </div>

                    <p style="text-align:center; color:rgba(255,255,255,0.4); font-size:13px; font-weight:500; margin-top:16px; margin-bottom:0;">Leve tudo isso por:</p>
                    <div class="price-val" style="margin-bottom:4px;"><span>12x R$</span>9<span style="font-size:24px; margin-top:10px;">,70</span></div>
                    <p style="font-size: 14px; color: rgba(255,255,255,0.4); margin-bottom: 28px;">ou <span style="color:#10B981; font-weight:700; font-size:16px;">R$ 97</span> à vista</p>

                    <a href="https://chk.eduzz.com/1W3ZJ8GPW2" class="btn-glow btn-green" id="btnPremium" style="width:100%;">Quero Meu Acesso Agora</a>

                    <p style="text-align:center; margin-top:16px; font-size:12px; color:rgba(255,255,255,0.3);">🔒 Pagamento seguro via Eduzz | Acesso imediato após confirmação</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 11. GUARANTEE ================= -->
    <section class="section-padding" style="text-align: center;">
        <div class="container reveal">
            <div class="guarantee-box">
                <div style="font-size: 64px; margin-bottom: 20px;">🛡️</div>
                <h2 class="section-title" style="margin-bottom: 20px;">Garantia Blindada<br>de 7 Dias</h2>
                <p style="color: var(--text-muted); font-size: 17px; max-width: 550px; margin: 0 auto 24px; line-height: 1.8;">
                    Assista à aula, aplique os 3 passos do Funil Invisível™. Se em 7 dias você não atrair <strong style="color:#fff;">pelo menos 1 cliente novo</strong> usando o método, basta apertar um botão e devolvemos <strong style="color:#10B981;">100% do seu dinheiro</strong>. Sem perguntas, sem burocracia.
                </p>
                <p style="color: rgba(255,255,255,0.35); font-size: 14px;">
                    O risco é todo nosso. Você não tem nada a perder.
                </p>
            </div>
        </div>
    </section>

    <!-- ================= 12. FAQ ================= -->
    <section class="section-padding">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-tag">Dúvidas</span>
                <h2 class="section-title">Perguntas Frequentes</h2>
            </div>

            <div class="faq-list reveal">
                <div class="faq-item">
                    <div class="faq-question">Funciona pra quem tem poucos seguidores? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">Sim. O método não depende de ter milhares de seguidores. Temos alunos que fizeram a primeira venda com menos de 500 seguidores. O Funil Invisível™ funciona porque atrai pessoas com intenção de compra — não importa o tamanho da audiência.</div></div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">Preciso investir em tráfego pago? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">Não. O método é 100% orgânico. Você não precisa gastar nenhum real com anúncios. Se quiser combinar com tráfego pago no futuro, pode — mas não é necessário para ter resultados.</div></div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">Em quanto tempo vou ver resultado? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">A aula tem 36 minutos. Você pode aplicar o primeiro passo no mesmo dia. Muitos alunos relatam resultados nos primeiros 3 a 7 dias. Depende da sua dedicação e do seu nicho, mas o método é projetado para gerar resultado rápido.</div></div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">Funciona pro meu nicho? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">O Funil Invisível™ funciona para qualquer pessoa que vende serviços, produtos físicos ou digitais. Já foi testado por dentistas, advogados, personal trainers, donos de loja, freelancers, coaches, nutricionistas e dezenas de outros nichos.</div></div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">E se eu não souber vender? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">Perfeito — o combo inclui a MasterClass de Vendas e o módulo de Fechamento Avançado justamente para isso. Você recebe scripts prontos de conversa para o WhatsApp. É só copiar, adaptar e usar. Não precisa de talento para vendas.</div></div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">Preciso de alguma ferramenta ou software? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">Não. Tudo que você precisa é um celular com Instagram e WhatsApp. Sem ferramentas pagas, sem plataformas complicadas, sem equipe. É você e seu celular.</div></div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">Como funciona a garantia? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">Você tem 7 dias para assistir a aula, aplicar o método e avaliar os resultados. Se por qualquer motivo não ficar satisfeito, basta solicitar o reembolso direto pela plataforma Eduzz. Devolvemos 100% do valor sem perguntas.</div></div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">O acesso é vitalício? <span class="faq-arrow">▼</span></div>
                    <div class="faq-answer"><div class="faq-answer__inner">Sim. Você compra uma vez e tem acesso para sempre. Assista quantas vezes quiser, no seu ritmo.</div></div>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 13. FINAL CTA ================= -->
    <section class="final-cta">
        <div class="container reveal">
            <h2 class="section-title" style="margin-bottom: 16px;">Pronto pra Atrair Clientes<br>Todos os Dias?</h2>
            <p style="color: var(--text-muted); font-size: 17px; max-width: 550px; margin: 0 auto 32px;">
                Por apenas <strong style="color:#10B981;">12x de R$ 9,70</strong>, você recebe o método completo + todos os bônus + garantia de 7 dias.
            </p>
            <div style="max-width: 420px; margin: 0 auto;">
                <a href="#pricing" class="btn-glow btn-green">Garantir Meu Acesso Agora</a>
            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer style="padding: 40px 0; text-align: center; border-top: 1px solid var(--border-light); color: var(--text-muted); font-size: 13px; line-height: 1.8;">
        <p>Este produto é comercializado com apoio da Eduzz. Este site não faz parte do Facebook ou Facebook Inc.</p>
        <p style="margin-top: 8px;">Todos os direitos reservados.</p>
    </footer>

    <!-- STICKY MOBILE CTA -->
    <div class="sticky-mobile-cta" id="stickyCta">
        <a href="#pricing" class="btn-glow btn-green" style="padding:16px; font-size:14px;">Quero Meu Acesso Agora</a>
    </div>

    <!-- ================= SCRIPTS ================= -->
    <script>
        // 1. SCROLL REVEAL
        const obs = new IntersectionObserver((entries) => {
            entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('active'); });
        }, { threshold: 0.1 });
        document.querySelectorAll('.reveal').forEach(el => obs.observe(el));

        // 2. STICKY MOBILE CTA
        const stickyCta = document.getElementById('stickyCta');
        const heroSection = document.getElementById('heroSection');
        const pricingSection = document.getElementById('pricing');
        let pastHero = false;
        let inPricing = false;

        function updateSticky() {
            if (pastHero && !inPricing) {
                stickyCta.classList.add('visible');
            } else {
                stickyCta.classList.remove('visible');
            }
        }

        if (window.innerWidth <= 768) {
            const stickyObs = new IntersectionObserver((entries) => {
                pastHero = !entries[0].isIntersecting;
                updateSticky();
            }, { threshold: 0.1 });
            stickyObs.observe(heroSection);

            const pricingObs2 = new IntersectionObserver((entries) => {
                inPricing = entries[0].isIntersecting;
                updateSticky();
            }, { threshold: 0.05 });
            pricingObs2.observe(pricingSection);
        }

        // 3. AUDIO PLAYERS
        let currentAudio = null;
        let currentPlayer = null;
        const speeds = [1, 1.5, 2];

        document.querySelectorAll('.wa-voice').forEach(player => {
            const btn = player.querySelector('.wa-voice__btn');
            const playIcon = player.querySelector('.icon-play');
            const pauseIcon = player.querySelector('.icon-pause');
            const timeEl = player.querySelector('.wa-voice__time');
            const durationEl = player.querySelector('.wa-voice__duration');
            const bars = player.querySelectorAll('.wa-voice__wave span');
            const dot = player.querySelector('.wa-voice__dot');
            const speedBtn = player.querySelector('.wa-voice__speed');
            const src = player.dataset.src;
            let audio = null;
            let speedIdx = 0;

            function formatTime(s) {
                const m = Math.floor(s / 60);
                const sec = Math.floor(s % 60);
                return m + ':' + String(sec).padStart(2, '0');
            }

            function resetPlayer() {
                player.classList.remove('playing');
                playIcon.style.display = 'block'; pauseIcon.style.display = 'none';
                bars.forEach(b => b.classList.remove('played'));
                dot.style.left = '0%';
                if (audio) { timeEl.textContent = formatTime(audio.duration || 0); audio.currentTime = 0; }
            }

            function updateProgress() {
                if (!audio || !audio.duration) return;
                const pct = (audio.currentTime / audio.duration);
                timeEl.textContent = formatTime(audio.currentTime);
                const playedCount = Math.floor(pct * bars.length);
                bars.forEach((bar, i) => bar.classList.toggle('played', i < playedCount));
                dot.style.left = (pct * 100) + '%';
                if (!audio.paused) requestAnimationFrame(updateProgress);
            }

            speedBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                speedIdx = (speedIdx + 1) % speeds.length;
                const spd = speeds[speedIdx];
                speedBtn.textContent = spd === 1 ? '1.0x' : spd === 1.5 ? '1.5x' : '2.0x';
                if (audio) audio.playbackRate = spd;
            });

            btn.addEventListener('click', () => {
                if (!audio) {
                    audio = new Audio(src);
                    audio.addEventListener('ended', resetPlayer);
                    audio.addEventListener('loadedmetadata', () => {
                        durationEl.textContent = formatTime(audio.duration);
                        timeEl.textContent = formatTime(audio.duration);
                    });
                }

                if (currentAudio && currentAudio !== audio) {
                    currentAudio.pause(); currentAudio.currentTime = 0;
                    currentPlayer.classList.remove('playing');
                    currentPlayer.querySelector('.icon-play').style.display = 'block';
                    currentPlayer.querySelector('.icon-pause').style.display = 'none';
                    currentPlayer.querySelectorAll('.wa-voice__wave span').forEach(b => b.classList.remove('played'));
                    currentPlayer.querySelector('.wa-voice__dot').style.left = '0%';
                }

                if (audio.paused) {
                    audio.playbackRate = speeds[speedIdx];
                    audio.play(); player.classList.add('playing');
                    playIcon.style.display = 'none'; pauseIcon.style.display = 'block';
                    currentAudio = audio; currentPlayer = player; updateProgress();
                } else {
                    audio.pause(); player.classList.remove('playing');
                    playIcon.style.display = 'block'; pauseIcon.style.display = 'none';
                }
            });
        });

        // 4. FAQ ACCORDION
        document.querySelectorAll('.faq-question').forEach(q => {
            q.addEventListener('click', () => {
                const item = q.parentElement;
                const wasOpen = item.classList.contains('open');
                document.querySelectorAll('.faq-item').forEach(i => i.classList.remove('open'));
                if (!wasOpen) item.classList.add('open');
            });
        });

        // 5. PIXEL EVENTS
        (function() {
            let fired30 = false;
            let firedPricing = false;

            window.addEventListener('scroll', function() {
                if (fired30) return;
                const scrollPct = (window.scrollY + window.innerHeight) / document.documentElement.scrollHeight;
                if (scrollPct >= 0.3) {
                    fbq('trackCustom', 'Visualizou30');
                    fired30 = true;
                }
            });

            const pricingEl = document.getElementById('pricing');
            if (pricingEl) {
                const pricingObs = new IntersectionObserver(function(entries) {
                    if (entries[0].isIntersecting && !firedPricing) {
                        fbq('trackCustom', 'VisualizouPricing');
                        firedPricing = true;
                        pricingObs.disconnect();
                    }
                }, { threshold: 0.2 });
                pricingObs.observe(pricingEl);
            }
        })();
    </script>
</body>
</html>
